import express from 'express';
const router = express.Router();

// Placeholder: serve a basic HTML or React frontend for browsing files
router.get('/', (req, res) => {
  res.send('<h1>File Browser Coming Soon</h1>');
});

export default router;
